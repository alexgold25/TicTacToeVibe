<Window x:Class="TicTacToeVibe.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TicTacToeVibe"
        Width="360" Height="420"
        Background="{DynamicResource Brush.Background}"
        Foreground="{DynamicResource Brush.Text}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <!-- ===== Theme ===== -->
    <Window.Resources>
        <!-- Palette -->
        <Color x:Key="Color.Background">#0F1116</Color>
        <Color x:Key="Color.Surface">#171B22</Color>
        <Color x:Key="Color.SurfaceHover">#1E2430</Color>
        <Color x:Key="Color.SurfacePressed">#11151C</Color>
        <Color x:Key="Color.Accent">#4F8CFF</Color>
        <Color x:Key="Color.AccentMuted">#2F5FB8</Color>
        <Color x:Key="Color.Text">#E6EAF2</Color>
        <Color x:Key="Color.TextMuted">#A9B0BD</Color>

        <!-- Brushes -->
        <SolidColorBrush x:Key="Brush.Background" Color="{StaticResource Color.Background}"/>
        <SolidColorBrush x:Key="Brush.Surface" Color="{StaticResource Color.Surface}"/>
        <SolidColorBrush x:Key="Brush.SurfaceHover" Color="{StaticResource Color.SurfaceHover}"/>
        <SolidColorBrush x:Key="Brush.SurfacePressed" Color="{StaticResource Color.SurfacePressed}"/>
        <SolidColorBrush x:Key="Brush.Accent" Color="{StaticResource Color.Accent}"/>
        <SolidColorBrush x:Key="Brush.AccentMuted" Color="{StaticResource Color.AccentMuted}"/>
        <SolidColorBrush x:Key="Brush.Text" Color="{StaticResource Color.Text}"/>
        <SolidColorBrush x:Key="Brush.TextMuted" Color="{StaticResource Color.TextMuted}"/>

        <!-- Base button with rounded corners + hover/press -->
        <Style x:Key="Button.Rounded" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
            <Setter Property="Background" Value="{StaticResource Brush.Surface}"/>
            <Setter Property="BorderBrush" Value="#00000000"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource Brush.SurfaceHover}"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource Brush.Accent}"/>
                                <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource Brush.SurfacePressed}"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource Brush.AccentMuted}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource Brush.TextMuted}"/>
                                <Setter TargetName="bd" Property="Background" Value="#12151B"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="#20242C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Cell button: bigger font, square-ish, subtle spacing -->
        <Style x:Key="CellButton" BasedOn="{StaticResource Button.Rounded}" TargetType="Button">
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="MinWidth" Value="72"/>
            <Setter Property="MinHeight" Value="72"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Make small gaps between cells -->
        <Style x:Key="CellContainerStyle" TargetType="ContentPresenter">
            <Setter Property="Margin" Value="4"/>
        </Style>
    </Window.Resources>

    <DockPanel Margin="12">
        <!-- Header -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12" VerticalAlignment="Center">
            <CheckBox Content="Human vs AI"
                      Foreground="{StaticResource Brush.Text}"
                      Margin="0,0,12,0"
                      VerticalAlignment="Center"
                      IsChecked="{Binding IsHumanVsAi}"/>
            <Button Content="New Game"
                    Style="{StaticResource Button.Rounded}"
                    Command="{Binding NewGameCommand}"
                    Margin="0,0,12,0"/>
            <TextBlock Text="{Binding StatusText}"
                       Foreground="{StaticResource Brush.TextMuted}"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Board 3x3 -->
        <ItemsControl ItemsSource="{Binding Cells}"
                      AlternationCount="9"
                      ItemContainerStyle="{StaticResource CellContainerStyle}"
                      Focusable="False">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="3" Columns="3"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Style="{StaticResource CellButton}"
                            Content="{Binding}"
                            Command="{Binding DataContext.CellClickCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            CommandParameter="{Binding (ItemsControl.AlternationIndex),
                                                       RelativeSource={RelativeSource AncestorType=ContentPresenter}}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DockPanel>
</Window>
